Prompt: I have an Allen & Heath AHM64 that needs to be able to control a Blustream sw42da, the AHM has a "Device Control" feature that can send simple TCP commands and is limited to boolean logic so it always expects to get a response from the device after it send a command, the SW42DA however comunicates via telnet and doesn't respond to commands, rather it has a status command that generates a relativley massive response that lists every variable on the device. I need to create a translation layer that lets these two devices comunicate. I have an API for the SW42DA (really just a list of telnet commands) in the project folder as well as some screenshot of the device control interface on the AHM. I would like this software to be very reliable, it might be nice to run it on a very simple device like a raspberry pi or even an ESP32 based microcontroller with ethernet like the M5Stack ESP32 Ethernet Unit with PoE, do let me know if this is a good idea or not.